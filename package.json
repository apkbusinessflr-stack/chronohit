{
  "name": "chronohit",
  "private": true,
  "version": "0.1.0",
  "description": "ChronoHit static site + API routes",
  "engines": { "node": "22.x" },
  "scripts": {
    "db:migrate": "node scripts/migrate.js",
    "db:status": "node scripts/migrate.js --status",
    "check:env": "node -e \"if(!process.env.DATABASE_URL){console.error('Missing DATABASE_URL');process.exit(1)}else{console.log('DATABASE_URL OK')}\""
  },
  "dependencies": {
    "pg": "^8.12.0",
    "stripe": "^16.0.0"
  }
}
